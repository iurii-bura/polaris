{
    "name": "polaris",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev:web": "npm --prefix ui run start",
        "dev:desktop": "concurrently -k -n WEB,ELECTRON -c cyan,yellow \"npm --prefix ui run start\" \"npm --prefix desktop run dev\"",
        "build:web": "npm --prefix ui run build",
        "build:desktop": "npm --prefix desktop run build",
        "build": "npm run build:web && npm run build:desktop",
        "package:mac": "npm run build && npm --prefix desktop run package:mac"
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}
