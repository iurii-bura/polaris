<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <!-- Required for V8 JIT compilation (Electron/Chromium renderer) -->
        <key>com.apple.security.cs.allow-jit</key>
        <true/>
        <!-- Required for Electron's renderer and Node.js internals -->
        <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
        <true/>
        <!-- Required for loading Electron's own frameworks -->
        <key>com.apple.security.cs.disable-library-validation</key>
        <true/>
        <!-- Allow access to files the user explicitly selects via open/save dialogs -->
        <key>com.apple.security.files.user-selected.read-write</key>
        <true/>
    </dict>
</plist>
